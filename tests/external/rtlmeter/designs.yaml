# RTLMeter design configuration for slang-netlist tests.
#
# Each top-level key is a unique test name. By default the rtlmeter design
# directory is derived from the test name; use the 'design' field to override
# this when multiple tests share the same design (e.g. different configs).
#
# Schema:
#   <TestName>:
#     enabled: true|false      # default: true
#     design: <DesignName>     # rtlmeter design directory (default: TestName)
#     config: <ConfigName>     # optional rtlmeter configuration to select
#     args:                    # optional extra slang-netlist flags
#       - <flag or file path>
#
# Entries in 'args' that do not start with '-' are resolved as file paths
# relative to the design's directory inside rtlmeter.

# --- Enabled designs ---

Example:
  args:
    - -DVERILATOR

NVDLA:
  args:
    - -DVERILATOR
    - -Wno-duplicate-definition

OpenTitan:
  args:
    - -DVERILATOR
    - --single-unit
    - src/prim_assert.sv
    - --relax-enum-conversions
    - -Wno-unconnected-port

VeeR-EH1:
  args:
    - -DVERILATOR
    - --single-unit

VeeR-EH2:
  args:
    - -DVERILATOR
    - --single-unit

VeeR-EL2:
  args:
    - -DVERILATOR
    - --single-unit

XiangShan-mini:
  design: XiangShan
  config: mini-chisel6
  args:
    - -DVERILATOR
    - --single-unit

XiangShan-full:
  design: XiangShan
  config: default-chisel6
  args:
    - -DVERILATOR
    - --single-unit

XuanTie-C906:
  args:
    - -DVERILATOR
    - --single-unit

XuanTie-C910:
  args:
    - -DVERILATOR
    - --single-unit

XuanTie-E902:
  args:
    - -DVERILATOR
    - --single-unit

XuanTie-E906:
  args:
    - -DVERILATOR
    - --single-unit

# --- Disabled designs ---

BlackParrot:
  enabled: false
  config: 1x1
  args:
    - -DVERILATOR
    - --allow-dup-initial-drivers

Caliptra:
  enabled: false

OpenPiton:
  enabled: false
  config: 1x1

Vortex:
  enabled: false
  config: mini
